<Window x:Class="VmManager.App.VmSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add virtual machines"
        Icon="Assets/AppIcon.ico"
        Width="520" Height="420"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" FontSize="16" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1"
                   Text="Only virtual machines that are not already in this group are shown."
                   Foreground="#666666" Margin="0,6,0,14" />

        <ListBox x:Name="VmListBox" Grid.Row="2"
                 SelectionMode="Extended"
                 SelectionChanged="VmListBox_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding State}"
                                   Foreground="#666666" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" Width="90" Height="32" IsCancel="True" />
            <Button x:Name="AddSelectedButton" Content="Add selected"
                    Width="110" Height="32" Margin="8,0,0,0"
                    IsDefault="True" IsEnabled="False"
                    Click="AddSelectedButton_Click" />
        </StackPanel>
    </Grid>
</Window>
