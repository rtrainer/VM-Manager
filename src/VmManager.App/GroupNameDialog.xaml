<Window x:Class="VmManager.App.GroupNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New VM group" Width="420" SizeToContent="Height"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="Manual" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Group name" Margin="0,0,0,8" />
        <TextBox x:Name="GroupNameTextBox" Grid.Row="1"
                 Height="32" Padding="8,0"
                 VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" Width="90" Height="32" IsCancel="True" />
            <Button Content="Create" Width="90" Height="32" Margin="8,0,0,0"
                    IsDefault="True" Click="CreateButton_Click" />
        </StackPanel>
    </Grid>
</Window>
