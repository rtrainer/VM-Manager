<Window x:Class="VmManager.App.GroupNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New VM group" Height="160" Width="380"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Group name" />
        <TextBox x:Name="GroupNameTextBox" Grid.Row="1" Margin="0,6,0,14" Height="28" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" IsCancel="True" />
            <Button Content="Create" Width="80" Margin="8,0,0,0" IsDefault="True" Click="CreateButton_Click" />
        </StackPanel>
    </Grid>
</Window>
